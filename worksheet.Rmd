---
title: "Worksheet"
output: html_notebook
---

```{r}
#Creating vector 1
vvec <- round(runif(100,1,6)) #Present lineup
vvec <- vvec[vvec %in% 1:6]
ltab <- table(vvec)

#Creating vector 2
yvec <- round(runif(100,1,6)) #Absent lineup
yvec <- yvec[yvec %in% 1:6]
ztab <- table(yvec)
```

```{r}
#Wells diagnosticity ratio calculated from vector/list
#Arguments: 2 vectors containing IDs from present/absent parade, suspect index
#vec 1: present, vec 2: absent. Must contain same suspect ID

diag_ratio_W <- function(vec1, vec2, x){
a <- sum(vec1 == x)/(length(vec1)+1)
b <-sum(vec2 == x)/(length(vec2)+2)
c <- a/b
}

print(wdiag_ratio(vvec, yvec, 4))
```
```{r}
#Tredoux diagnosticity ratio calculated from vector/list

diag_ratio_T <- function(vec1, vec2, x){
a <- (sum(vec1 == x)+0.5)/((length(vec1)+1)+0.5)
b <-(sum(vec2 == x)+0.5)/((length(vec2)+2)+0.5)
c <- a/b
}

```


#VARIANCE OF DIAGNOSTICITY RATIO
```{r}
#Create vectors
var_diag_ratio <- function(vec1, vec2, x){
    a <- sum(vec1!= x)
    b <- (sum(vec1 == x))*(length(vec1)+1)
    c <- sum(vec2 != x)
    d <- (sum(vec2 == x))*(length(vec2)+2)
    e <- (a/b)+(c/d)
}

ratio_weight <- function(vec1, vec2, x){
    a <- sum(vec1==x)*sum(vec2==x)*
        (sum(length(vec1)+1))*(sum(length(vec2)+2))
    b <- 
}
```

```{r}
sum(length(yvec)+1)
```


